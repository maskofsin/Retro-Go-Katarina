cmake_minimum_required(VERSION 3.5)
set(COMPONENTS "main retro-go pcsx")
if(NOT DEFINED RG_PROJECT_APP OR "${RG_PROJECT_APP}" STREQUAL "")
    set(RG_PROJECT_APP "psx")
endif()
if(NOT DEFINED RG_PROJECT_VER OR "${RG_PROJECT_VER}" STREQUAL "")
    set(RG_PROJECT_VER "1.1")
endif()
if(NOT DEFINED RG_BUILD_TARGET OR "${RG_BUILD_TARGET}" STREQUAL "")
    set(RG_BUILD_TARGET "RG_TARGET_KATARINA")
endif()
if(NOT DEFINED RG_BUILD_RELEASE OR "${RG_BUILD_RELEASE}" STREQUAL "")
    set(RG_BUILD_RELEASE 0)
endif()
set(RG_ENABLE_NETWORKING 0)
include(../base.cmake)
project(psx)
